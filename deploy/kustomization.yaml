apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: spalce-credaction-supabase

resources:
  - namespace.yaml
  # Uncomment to use example secrets (not recommended for production)
  # - secrets-example.yaml

# For production, use a secret management solution like:
# - Sealed Secrets: https://github.com/bitnami-labs/sealed-secrets
# - External Secrets Operator: https://external-secrets.io/
# - SOPS: https://github.com/mozilla/sops

# You can also use generators to create secrets from files
# secretGenerator:
#   - name: supabase-jwt
#     files:
#       - jwt-secret.txt
#   - name: supabase-db
#     literals:
#       - password=YOUR_PASSWORD
#       - database=postgres
